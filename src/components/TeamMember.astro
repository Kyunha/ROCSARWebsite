---
interface Props {
  name: string;
  role: string;
  bio: string;
  skills: string[];
}

const { name, role, bio, skills } = Astro.props;
---

<div class="card">
  <div class="header">
    <h3>{name}</h3>
    <span class="role">{role}</span>
  </div>
  <p class="bio">{bio}</p>
  <div class="skills">
    {skills.map((skill) => <span class="tag">{skill}</span>)}
  </div>
</div>

<style>
  .card {
    background: #181825;
    border: 1px solid #313244;
    border-radius: 12px;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    transition: transform 0.2s ease, border-color 0.2s ease;
  }
  .card:hover {
    transform: translateY(-4px);
    border-color: #89b4fa;
  }
  h3 {
    margin: 0;
    color: #cdd6f4;
    font-size: 1.25rem;
  }
  .role {
    color: #89b4fa;
    font-size: 0.875rem;
    font-weight: 600;
    display: block;
    margin-top: 0.25rem;
  }
  .bio {
    color: #a6adc8;
    font-size: 0.9rem;
    line-height: 1.5;
    margin: 1rem 0;
  }
  .skills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }
  .tag {
    background: #313244;
    color: #cba6f7;
    font-size: 0.75rem;
    font-family: monospace;
    padding: 0.25rem 0.5rem;
    border-radius: 6px;
  }
</style>
